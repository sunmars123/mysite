字符串格式化输出
    1.占位符
        %s s = string 字符型
        %d d = digit 整数
        %f f = float 浮点型
    2.format函数
数据运算
    数据类型初识
        数字
            整数  int
                整型
                长整型
        布尔 只有2中状态，分别是
            真 True
            假 False
        字符串
            isdigit()判断字符串是否全由数字组成
            计算机中，一切皆为对象
            世界万物，皆为对象，一切对象皆可分类（面向对象核心原理）
循环：
    死循环
    次数循环
    中断循环：break
    跳出循环: continue
注：当多层循环时，可以通过标志位去控制层级与层级之间的关联，flag
for：
    ...
else:
    ...
执行for else语句时，循环正常执行完毕，中间没被打断，就不会执行else
